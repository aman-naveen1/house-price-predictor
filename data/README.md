# Data

The California Housing dataset is downloaded automatically by scikit-learn when `train.py`, `app.py`, or the notebook is run.

No dataset file is committed here because the project uses scikit-learn's dataset loader directly.

The dataset contains 20,640 observations and eight numeric input features, with median house value as the target.
